import { DMButton, DMStatus, DMWrapper } from "./styled";

const DarkMode = () => {
    return (
        <DMWrapper>
            <DMStatus>dark mode on</DMStatus>
            <DMButton>SUN</DMButton>
        </DMWrapper>

    );
};

export default DarkMode;